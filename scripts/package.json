{
  "name": "samantha-website-checks",
  "private": true,
  "type": "module",
  "description": "Dev-only browser checks. The site itself stays dependency-free.",
  "scripts": {
    "check": "node mobile_check.mjs",
    "edit": "node copy_editor.mjs",
    "test:editor": "node copy_editor.mjs --self-test && node copy_editor_check.mjs"
  },
  "devDependencies": { "playwright": "^1.50.0" }
}
